[Unit]
Description=TrikMed Voice Recorder
After=network.target

[Service]
WorkingDirectory=/opt/trikmed-recorder
ExecStart=/opt/trikmed-recorder/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
